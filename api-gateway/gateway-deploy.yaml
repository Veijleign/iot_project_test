apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-gateway-deployment
spec:
  replicas: 1 # for start test
  selector:
    matchLabels:
      app: api-gateway
  template:
    metadata:
      labels:
         app: api-gateway # how Deployment and Service find each others
    spec:
      containers:
        - name: gateway-container
          image: api-gateway:v2
          imagePullPolicy: Never # To user local images
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: api-gateway-config
            - secretRef:
                name: api-gateway-secret

---
apiVersion: v1
kind: Service
metadata:
  name: api-gateway # inside cluster DNS name for CoreDNS
spec:
  selector:
    app: api-gateway # where to send requests
  ports:
    - port: 80
      targetPort: 8080